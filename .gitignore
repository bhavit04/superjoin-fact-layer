.venv/
__pycache__/
*.pyc
.env
data/*.db
data/*.db-wal
data/*.db-shm
data/uploads/
assignment.pdf
.pytest_cache/
.DS_Store

# The recording under build/ is the deliverable; everything that produces it --
# raw captures, intermediate frames, the compositor -- is local build tooling.
build/stage/
build/rec/
build/work/
build/frames/
scripts/video/
